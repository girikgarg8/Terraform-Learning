resource "aws_security_group" "rds_proxy" {
  name_prefix = "mariadb-proxy-"
  description = "Allow MySQL/MariaDB client to RDS Proxy (VPC CIDR)"
  vpc_id = aws_vpc.main.id

  ingress {
    from_port   = 3306
    to_port     = 3306
    protocol    = "tcp"
    cidr_blocks = [var.vpc_cidr]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}

resource "aws_security_group" "rds" {
  name_prefix = "mariadb-rds-"
  description = "Allow MySQL/MariaDB from RDS proxy"
  vpc_id      = aws_vpc.main.id

  ingress {
    from_port       = 3306
    to_port         = 3306
    protocol        = "tcp"
    security_groups = [aws_security_group.rds_proxy.id]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}

# Read replica: allow direct connections from any IP in VPC (for read traffic).
resource "aws_security_group" "rds_replica" {
  name_prefix = "mariadb-rds-replica-"
  description = "Allow MySQL/MariaDB to read replica from VPC CIDR"
  vpc_id      = aws_vpc.main.id

  ingress {
    from_port   = 3306
    to_port     = 3306
    protocol    = "tcp"
    cidr_blocks = [var.vpc_cidr]
  }

  egress {
    from_port   = 0
    to_port     = 0
    protocol    = "-1"
    cidr_blocks = ["0.0.0.0/0"]
  }
}